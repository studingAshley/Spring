<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.www.mapper.BoardMapper">

	<select id="bList" resultType="com.java.www.dto.BoardDto">
		select * from board order by bgroup desc, bstep asc
	</select>
	
	<select id="bView" resultType="com.java.www.dto.BoardDto">
		select * from board where bno=#{bno}
	</select>
	
	<insert id="bInsert">
		insert into board values(board_seq.nextval, #{btitle}, #{bcont}, sysdate, #{id}, board_seq.currval, 0, 0, 0, #{bfile} ) 
	</insert>
	
</mapper>